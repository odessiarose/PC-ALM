[project]
name = "pc-alm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "odessiarose", email = "321957247+odessiarose@users.noreply.github.com" }
]
requires-python = ">=3.12"
dependencies = [
    "numpy<2",
    "torch==2.2.1",
]

[project.scripts]
pc-alm = "pc_alm:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
